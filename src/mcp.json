{
  "servers": {
    "expense-route-queue": {
      "type": "http",
      "url": "$QUEUE_MCP_SERVER_URL",
      "tools": ["*"],
      "auth": {
        "scope": "https://apihub.azure.com/.default",
        "client_id": "$QUEUE_MCP_CLIENT_ID"
      }
    }
  }
}
