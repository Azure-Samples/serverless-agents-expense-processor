# Standalone Azure Functions serverless agents runtime + optional Azure Monitor exporter.
# The distribution is azurefunctions-agents-runtime; the import package is azure_functions_agents.
azurefunctions-agents-runtime[monitor]==0.1.0b6

# The route_expense_decision tool (src/tools/route_decision.py) writes the agent's decision to the
# destination queue with the Azure Queue Storage SDK, and get_expense_policy (src/tools/get_policy.py)
# reads the approval policy document from Blob Storage. In the cloud both authenticate with the
# Function app's managed identity (DefaultAzureCredential); locally they use the Azurite connection
# string. The demo helper scripts declare their own deps in scripts/requirements.txt.
azure-storage-queue>=12.17.0
azure-storage-blob>=12.19.0
azure-identity>=1.19.0
